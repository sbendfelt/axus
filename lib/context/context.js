class Context {
    constructor(code, sandbox, configurations) {
      this.code = code;
      this.sandbox = sandbox;
      this.configurations = configurations;
      this.name = undefined;
    }

}
